//Ejercicios:A
//1. Crea un archivo llamado utils.js con una función llamada restar(a, b) que retorne la resta
//de dos números.
//2. Crea un archivo principal (main.js), importa esa función y úsala ejecutar varias pruebas.

import { restar } from './utils.mjs';

console.log(restar(0,0));
console.log(restar(30,14));
console.log(restar(54,28));
